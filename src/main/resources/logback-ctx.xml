<?xml version="1.0" encoding="UTF-8"?>
<included>
	<property name="HOME_LOG" value="." />

	<appender name="CTX-LOG" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${HOME_LOG}/ctx.log</file>
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<pattern>
				%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg %n
			</pattern>
		</encoder>

		<rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<fileNamePattern>${HOME_LOG}/history/central-textilera.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
			<maxFileSize>500MB</maxFileSize>
		</rollingPolicy>
	</appender>

	<root level="INFO">
		<appender-ref ref="CTX-LOG" />
	</root>
</included>
